@model IEnumerable<Sistema_Advocacia.Models.NaturezaAcao>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de Tipos de Ações</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RamoDireito)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comentario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RamoDireito)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comentario)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.NaturezaAcaoID }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.NaturezaAcaoID }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.NaturezaAcaoID })
        </td>
    </tr>
}

</table>
